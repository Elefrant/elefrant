'use strict';

// TODO: stats
// Add functionality PIWIK, GOOGLE ANALITYCS and ELEFSTATS

/*module.exports = function (server, restify, bunyan, config) {
 *
 *
 *
 *
 *
 *
 * // Handler that run after execute the route
    server.on('after', function (req, res, route, err) {
        // Log request
        if (config.system.debug) {


            var latency = Date.now() - req.time();
            console.log('%s %s %s %sms - %s', req.method, req.url, res.statusCode, latency, res.get('Content-Length'));


            console.log(route);
            console.log(res);
            req.log.info({
                x: 'req'
            }, 'Request');
        }
    });

};*/
