'use strict';

/**
 * Audit Logger configuration properties
 */
module.exports = {
    /*
     * Enable Audit
     * --------------------------
     * Enable Audit Logger
     */
    enable: true,
};
