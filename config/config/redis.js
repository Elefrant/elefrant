'use strict';

/**
 * Redis configuration properties
 */
module.exports = {
    /*
     * Host
     * --------------------------
     * Host of database
     */
    host: 'localhost',

    /*
     * Port
     * --------------------------
     * Port of database
     */
    port: '6379',

    /*
     * Options
     * --------------------------
     * Options for redis
     */
    options: null,

    /*
     * Password
     * --------------------------
     * Auth password to connect to redis
     */
    password: null,

    /*
     * Database
     * --------------------------
     * Number of database
     */
    database: null
};
